<div class="row">
  <%= render 'enduser/genres/genre_search', genres: @genres %>

  <h2>顧客側の商品詳細</h2>
  　<div class="col-xs-3">
  　  <label>商品画像</label>
　　　<%= attachment_image_tag @item, :image, :fill, 250, 175, fallback: "no_image.jpg", size: "250x175" %>
　　</div>
    <div class="col-xs-3">
      <div class="item name">
        <label>商品名</label>
        <%= @item.name %><br>
      </div>
      <div class="item details">
        <label>商品説明</label>
        <%= @item.details %><br>
      </div>
      <div class="item price_before_tax">
        <label>税込価格</label>
        ¥<%= @price_includ_tax %> (税込)<br>
      </div>
    </div>
    <div class="col-xs-4">
      <%= form_with model: @cart_item, url: enduser_cart_items_path, local: true do |f| %>
        <%= f.select :auantity,(1..50), class: "new_cart_item" %>個
        <%= link_to 'カートに入れる', enduser_cart_items_path, class: "btn btn-primary" %>
      <% end %>
    </div>
</div>

